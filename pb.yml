---
- hosts: dev
  become: True
  tasks:
    - name: Install docker
      yum:
        name: docker
        state: present
    - name: start docker
      service:
        name: docker
        state: started
        enabled: yes
    - name: Start the container
      docker_container:
        name: swapgops
        image: "swapgops/webapp:${{DOCKER_TAG}}"
        state: started
        published_ports:
          - 0.0.0.0:8081:8080
